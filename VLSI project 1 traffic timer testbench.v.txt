`timescale 1ns / 1ps

module tb_traffic_light;

reg clk;
reg clr;
wire [5:0] lights;

Traffic_Light_Time uut(
    .clk(clk),
    .clr(clr),
    .lights(lights)
);

initial begin
    $dumpfile("dump.vcd");
    $dumpvars(0, tb_traffic_light);

    clk = 0;
    clr = 1;

    #20 clr = 0;

    #500 $finish;
end

always #10 clk = ~clk;

endmodule